## Problem: Log files and Configurations

- We "go" to a server with `ssh` or `putty`
- We go into a container with `docker run -it...` or `docker exec -it...`
- We need to look around and evaluate log files
- Could we copy the stuff to our local machine? But that's annoying!  <!-- .element class="fragment orange" -->
- So what do we do?  <!-- .element class="fragment blue" -->
    <span class="fragment c-orange">look in the terminal, search, ...</span>
    <span class="fragment c-blue">but how????</span>

Here come the commands, pagers and editors <!-- .element class="fragment" -->
`tail`, `less`, `vi`, `nano`
