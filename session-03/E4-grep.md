## Tasks for grep

Preparation: run the command `sudo start-logging.sh`.

- how do I find all lines from file `obelix.haus` that contain `ix`
- how do I find all lines from file `obelix.haus` that contain `as`, regardless of uppercase or lowercase
- how do I find with grep all files in your `$HOME` directory that contain `ix`
- how do I find with grep all files in your `$HOME` directory that contain `As` (with and without uppercase/lowercase)
- show me all lines from the file `/var/log/fake-service.log` that:
    - contain the string `OK`
    - contain the string `proc`
    - do not contain the string `ERR`
